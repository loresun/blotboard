# 变更记录

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.4] - 2026-09-09

### 修复：源码放错位置静默变空白卡、超长图被砍半、视口没法单独调、批量落卡无幂等

一次落 20 张 SVG 卡时踩出来的静默失败。共同点是**服务端 201、卡面却不对，
调用方看不出哪一步吞的**——都改成当场点名；顺带补上批量落卡与自动摆位。

- **顶层 `source` 被静默吞掉**：agent 最顺手的写法 `{"type":"svg","source":"<svg…>"}`，
  卡包 schema 只读 `input.svg`，顶层 `source` 从头到尾没人看 → 落库成功、卡面空白。
  新增 `assertSourceNotTopLevel`（`lib/normalize-base.ts`），svg / mermaid / code /
  excalidraw 四类卡的 `onCreate` / `onConvert` / `onPatch` 一律先过闸门，误传时
  400 并给出正确写法。**规格卡（data）不在此列**——它的 `source` 合法位置在
  `data.source` 里（app / url / externalId），所以闸门只加在四类源码卡上。
- **超上限静默截断**：`MAX_DIAGRAM_SOURCE`（40 000）在 svg / mermaid 上是
  `.slice(0, MAX)`——截一半的 SVG 不是良构 XML，卡面直接裂图。改为与
  code / excalidraw 卡同一条口径：超限抛 400 并报出实际字符数。
- **`board_layout` 没法只调视口**：服务端 `PUT /api/boards/{id}/state` 早就支持
  纯 `viewport`，但 MCP 工具的 inputSchema 里没有这个字段，handler 也在
  `mode` 缺失时直接抛「mode 与 cards 至少给一个」——只能绕到 `PATCH /api/boards/{id}`。
  工具层补上 `viewport` 入参与分支（不重排、不动任何卡片坐标），报错文案同步。
- **批量落卡没有幂等入口**：批量新建只能 for 循环打 N 次单卡接口（N 次往返 + N 次
  落盘），而现有的 `/paste` 一律换新 id、重试必重复。`POST /api/boards/{id}/cards`
  现在认 `{cards:[…], onDuplicate?}`：一次事务落一批，**幂等靠调用方自带的 `c_` id**
  ——命中已有 id 默认进 `skipped`（不报 409），原样重试安全；`onDuplicate:"create"`
  则换新 id 再建一张。返回 `{cards, created, skipped, updatedAt}`。
- **无坐标卡全叠在 (80,80)**：`normalizeCardInput` 对缺省坐标一律给 80,80，连发几张
  就全叠在一处，肉眼只看到最后一张。把信封导入里的网格摆位抽成 `lib/auto-place.ts`，
  单卡建卡 / 批量建卡 / 信封导入共用一份规则（只碰 x/y **双缺省**的卡）。同时把
  `grid` 整理模式的说明补上「会覆盖已有卡片坐标」——它一直是全量重排，只是文档没说透。

回归：`npm run check` 全绿；`scripts/smoke-api.mjs` 新增四组断言——四类卡顶层
`source` → 400 且嵌套写法仍 201、svg/mermaid 超限 → 400 报实际数字、
`board_layout` 纯 `viewport` 生效且卡片数不变、批量建卡 3 张落 3 处坐标 +
带 id 重试 `created:0 / skipped:2` + `onDuplicate:"create"` 换新 id。
线上实例逐条复验通过。

## [1.0.3] - 2026-09-08

### 修复：图片多的画板把界面占死，图片加载期间整个服务变慢

一块 10 张 2048×1152（单张 ≈2 MB）image 卡的板，打开后左栏点不动，期间连刷新
画板导航都慢。GA-75 只读根因分析（三处证据均带 `文件:行`）定位出三个叠加因素，
本次一并修掉：

- **image 卡面没有 lazy 加载**：卡面 `<img>` 裸奔，进板即全部挂载，一屏十几张
  大图同时解码 + paint 全压在 main thread 上——这是「界面被占住」的真因。
  补上 `loading="lazy"` + `decoding="async"`，与 BookCover / DiagramCard /
  HtmlCard 同一口径；摊开阅读的 FullView 保持 eager（点开就是要看大图）。
- **图片预览口同步整读**：`/api/uploads/[id]/preview` 走 `readFileSync` 把整份
  图读进内存再回（单次 ≈25ms 事件循环空窗），十张并发期间其它请求（含侧栏
  `/api/boards`）全在排队。新增 `openControlledImage`：校验口径一道不少
  （id 格式 + realpath + 软链 + 大小上限 + 头部 32 字节真实签名），正文改
  `createReadStream` 异步吐；`statControlledImage` 顺带带回 `size` 供
  content-length。导出打包用的 `readControlledImage` 整读保持不变（base64
  内联需要全字节）。
- **侧栏全量清单没有协商缓存**：列表随板数线性涨（830+ 块 ≈200 KB），前端
  每分钟 poll 一次、每次写板后还会重拉。`GET /api/boards` 加 weak ETag
  （板数 + 最大 updatedAt——写路径一律 bump updatedAt，不会漏报更新），
  `cache-control: private, no-cache`（每次回源验证，无 staleness 窗口），
  命中回 304。

回归：tsc 通过、API 冒烟全过；preview 与流式下载路由 sha256 逐字节一致，
304 / 404 拒绝路径完好；并发 10 张 preview 47ms → 35ms，侧栏二次协商
304 ≈ 5ms / 0 字节。

## [1.0.2] - 2026-09-08

### 修复：任务抽屉把本机 ACP run 当成对端的任务，给出一条死链

1.0.1 上线后立刻撞到的：本机跑完的那一轮，任务抽屉照样显示「在主界面查看」，
点过去是 Goal Agent 的 `?task=r_...`——**对端 Runner 根本没有这条 task**。

- 「这个 task id 是不是本机 run」的判定原来只写在任务台里，抽屉那两处各自没有，才漏成死链。
  现在收进 `lib/constants.ts` 的 `isLocalRunId()`，三处共用一份；
- 本机 run 的抽屉改成「**在任务台查看**」，直达画板自己的任务台
  （流式 transcript、权限确认、中止都只在那里）；对端的 task 才跳 Runner 主界面；
- 抽屉里同时标出「本机 · <agent>」，跑在哪一边一眼可见；
- 产出那栏不再拿本机 run 的 id 去问对端要产出（对端不认识它），文案也改成
  「产出登记归外部 Runner」，而不是让人误以为 agent 没干活。

回归：e2e 新增一条，断言本机 run 的抽屉里出现「在任务台查看」且**没有**「在主界面查看」——
把 `isLocalRunId` 改回恒假能让它红，确认不是一条摆设用例。

## [1.0.1] - 2026-09-07

**主题：把「派给本机 agent」从能跑变成好用，并且不再挑任务后端。**

起因是一轮针对 ACP 派单的实测：真的 Claude Code 拿到 prompt 之后，在文件系统里翻了 150 秒
找回写用的 token 也没开工。顺着这条线查下去，问题不止一处。

### ACP agent 拿得到回写凭证了（原来它只能靠猜）

- prompt 里写着「token 取环境变量 `BLOTBOARD_INTERNAL_TOKEN`，或读数据目录下的 token 文件」，
  但 spawn 的 env 里**根本没有这个变量**（默认部署走自管 token 文件），而数据目录的绝对路径
  又刻意不写进 prompt（它会随免鉴权的 `GET /api/issues/:id` 吐出去）——三头堵死。
  现在 spawn 时注入 `BLOTBOARD_INTERNAL_TOKEN` 与 `BLOTBOARD_API_BASE`，
  **只走 env 这一条不经过任何响应体的通道**：token 不进 prompt、不进 `issues.json`、不进任何 API 响应。
- agent 起不来时，transcript 原来只有一句「agent 进程意外退出（code 1）」，真实原因看不到。
  现在 stderr 进环形缓冲（200 行），run 到终态取最后 ≤10 行**脱敏后**落一条 transcript
  （本机路径换占位、疑似密钥整段抹掉），failed 的备注末尾也带上最后一行原因。
  `BLOTBOARD_ACP_STDERR=full` 时另把原文全量写到 `<data>/runs/<runId>.stderr.log`（0600）。
- agent 缺 API key 时回的 `-32000` 原来显示成「协议错误码 -32000」，没人知道该去哪配。
  现在直说：「该 agent 需要认证：请在任务台 → Runner 设置为它配置 API key」。

### Runner 设置带上了内置预设与连通性检测

手抄一行命令行赌它能跑，是这块原来的全部体验。两个断层让这件事比看上去难：**装了 CLI 不等于
它说 ACP**（Claude Code / Codex / pi 都要另跑一个适配器进程），**说了 ACP 不等于能开会话**
（没登录会在握手里回 `-32000`）。

- 内置五条预设（Claude Code / Codex / Kimi CLI / opencode / pi），每条带首选命令 + npx 兜底；
- 新增 `POST /api/runner-settings/probe`：用**派单时的同一套握手**真跑一次
  （spawn → `initialize` → `session/new` → 立刻杀），**不发 prompt**，不花 token、不动文件。
  失败时说清卡在哪一步：`spawn` = 命令不在 PATH 里、`initialize` = 起来了但不说 ACP、
  `session` = 说 ACP 但开不了会话；
- 面板上的「检测并添加」**探通了才落盘，落的是探通的那条命令**——首选二进制没装就落 npx 兜底那条，
  所以注册表里存的一定是这台机器上真能跑的形态，而不是文档里的理想形态；
- 适配器一律用 `@latest` 而不钉版本：钉旧版会以「握手超时」这种最难查的形态失败。

### 本机 ACP 派单不再挑任务后端

`agentId` 原来只有 local 后端消费，goal-agent / http 后端下被直接丢弃——结果是**已经接了外部
Runner 的部署反而用不上「一键派给本机 Claude Code」**，最需要这个能力的人用不了。现在它是一条
与 `tasks.backend` 正交的执行通道（`lib/integrations/acp-lane.ts`）：

- **Issue 真源不动**。派单前先从对端取回**此刻**的标题正文（不是转 Issue 时那份旧快照），
  本地只开一条带 `external` 标记的**执行台账**挂 run / transcript / 权限请求。台账不进 Issue 列表、
  不参与 Issue 同步——不造第二本账；同一条远程 Issue 的多次本机执行记在同一条台账上；
- **run id 前缀即路由**：本机 run 一律 `r_` 开头，任务状态查询命中就地服务，否则打对端。
  外部 Runner 的执行与本机执行各自记账、互不干扰；
- **不带 `agentId` 时远程链路零变化**：请求体逐字节照旧发给对端；
- `/api/issues/*` 那道「非 local 后端一律 501」的整体闸拆成三道——它原来会把本机 run 的回写口
  一起挡死，agent 一开工就 501。现在 Issue 真源类操作仍 501，单条 Issue 只放行本机执行台账，
  run 级操作（回写 / transcript / 权限确认）只要本机查得到就放行。

任务台（聚合镜像形态）的呈现取舍：本机 run **不另开列表**——它不是「另一类任务」，而是同一条
任务的另一个执行者。所以是同一行打一枚「本机 ACP」标记、左栏多一个横切镜头「本机执行」、
详情里并列一块「本机执行」区（agent 下拉 / 派单 / 流式 transcript / 权限确认 / 中止）。
最后那块恰好是外部 Runner 那条路给不了的，也正是两条路值得分开呈现的全部理由。

**来源卡的状态现在跟着本机 run 走**（completed → 已完成，failed / aborted → 已建 Issue）：
这条 run 的生命周期归画板所有，就有义务让卡片别一直停在「执行中」。外部 Runner 那条路画板只是
镜像，卡片上存的状态不归它推动，维持原样。

### 顺带修掉的几处

- **脱敏在 macOS 上会漏**：`/var` 是 `/private/var` 的软链，子进程报的是 realpath，
  只按原样路径替换会留下 `/private<工作目录>` 这样的残片。现在两种写法一起换。
- **ACP 握手超时从 30s 提到 60s**：实测 codex-acp 冷启动到 `session/new` 要 22 秒，
  原来只剩 8 秒余量，机器一忙就会以「握手超时」假失败（npx 首次下载适配器还要再叠十几秒）。
- 脱敏规则从 `lib/acp/manager.ts` 抽成共用模块，检测与 stderr 摘要用同一份，免得一边补了另一边漏。
- 落地页里指向真实仓库的 GitHub 链接被 `lint:repo` 当成作者身份泄漏——那和 `package.json` 的
  `repository` 是同一个地址、同一个理由，按检查自己的指引加进 `PRIVATE_ALLOW` 并写明原因。

## [1.0.0] - 2026-09-07

**首个公开发布版本。** 此前的开发在私有仓库里进行，公开仓库从这一版的完整源码起步、
不携带私有历史——所以这里没有 1.0.0 之前的版本条目，下面按主题记的就是这一版交付的东西。

1.0.0 不代表「不会再变」，只代表从这一版开始按语义化版本承诺：
破坏性改动进大版本，数据格式与 API 的变更会写在这里。

### 跨平台：顶栏收纳零容忍溢出，仓库体检不再把 CI 账号名当成作者身份

- 顶栏的升档判定原本留了 1px 容差，「差一点点装不下」时它认为装得下，于是停在当前档、
  让最右边那颗按钮探出可视区 1px。本机测不出来——字体宽度按平台差一点点，同一档
  macOS 量出 900、Linux 量出 901。现在对溢出零容忍，多收一档（按钮在「更多」里一件不少）。
- `lint:repo` 的身份检查会把**操作系统用户名**当作者标识来查，而 GitHub Actions 的系统
  用户名就叫 `runner`——恰好是本项目的核心概念，全仓 239 处 "runner" 被判成身份泄漏。
  系统账号名本就只是启发式猜测，现在撞上通用服务账号名（runner / root / ubuntu / …）就弃用。

### 存储方式：明确推荐服务端，并说清浏览器模式下 agent 的能力边界

- 两处选存储的地方（顶栏左上角的存储面板、`/start` 首启页）都给**服务端文件**标上「推荐」。
  两种存法一直是 50/50 并排的，可它们对 agent 的开放程度差得很远，并列等于让人凭手感挑。
- 存储面板底部那段「本地 Agent 怎么控制？」原本**无条件**讲 Playwright/CDP —— 可那只对
  浏览器模式成立。服务端模式下照着做，等于放着 HTTP API / MCP / Skill 不用，绕去接管标签页。
  现在按当前模式分岔：服务端讲 `/api/skill` + token + MCP，浏览器讲 CDP。
- 浏览器模式下补一条能力边界：没有 HTTP API 也没有 MCP，只有本来就会控制浏览器的 agent
  用得了；上传件、快照、Runner 与 HTML / PDF 排版导出在这个模式下都没有。

### npm 包不再多带 13 MB 字体

- `files` 白名单不认 `.gitignore`，`public/excalidraw-assets/`（237 个字体文件）会被打进
  tarball —— 而它本来就由 `postinstall` 从 `node_modules` 现同步，装包的人自己会生成一份。
  排除之后包从 **14.5 MB 降到 1.3 MB**（493 个文件）。

### 阅读模式的缩放尺度放宽

- **10% – 1000%**（原来是 25% – 400%）。上限卡在 4 倍时，摊开一张子画板卡等于把整块板压进一个舞台，
  里面的卡片标题只有几个像素高，推到头也认不出字；下限卡在 25% 时，长图又退不到能看见全貌的位置。
  这几类里除 image 外全是矢量（SVG / mermaid / 导图 / 子画板），放多大都不糊，没有理由拦着。
- **滚轮与触控板捏合改走连续值**：以前每个 wheel 事件硬跳一整档，而捏合是一串「每次几个像素」的事件——
  手指刚碰上去就冲到头，中间那些尺度一个都停不住。现在按位移指数缩放（一格滚轮约 ±18%，
  捏合细到百分之几），档位只留给按钮与 `+` / `-`：那里一下按一档才顺手。
- 配套三处：档位表在 100% 附近加密、两头放疏（`…0.8 · 1 · 1.25 · 1.5 · 2 · 2.5 · 3 · 4 · 5 · 6 · 8 · 10`）；
  按钮从任意值（比如捏出来的 137%）**按到严格更小 / 更大的那一档**，不再先往回跳一格；
  到顶到底时对应那颗按钮变灰，免得看着像按钮坏了。
- 顺带修掉滚轮单位：`deltaMode` 为「行」/「页」时（Firefox 常给「行」）以前按像素直接用，
  平移和缩放都慢得像没反应，现在统一换算成像素。
- 回归：e2e 那条阅读模式用例补上「一下滚轮落在两档之间」「+/- 一路按到 1000% / 10% 并变灰」
  「从非档位值按一下落到更小的那一档」。

### 数据迁移完整性与恢复交互

- **导出不再静默少给板**：`selectBoards` 攒够 200 块就 `break` 的老实现，让一个 201 块板的分组
  导出去只有 200 块、HTTP 还是 200、`notes` 还是空的——用户手里那份「成功的备份」是残的，
  而残不残只有真去恢复那天才知道。现在挑板**永远给出完整的那一批**（子板递归与环状引用照旧由
  去重挡住），装不下就**分卷**：`?plan=1` 只看计划不打包，`?volume=N` 取第 N 卷，
  不给 `volume` 又超限则 **413 并附上整个分卷计划**（总数 / 几卷 / 每一卷的取回地址）。
  每一卷都是完整可导入的画板包，卷号写在 `bundle.volume` 里；`format=html` / `md` 同一条口径。
  界面上的「备份整个画板库」跟着分卷下载多个文件，toast 明说下了几卷、共多少块板。
- **浏览器库收得住附件字节**：以前带图的画板包导进浏览器库会「成功」，但卡面是「图片缺失」，
  再备份一次连 assets 都是空的——原始字节已经没了。现在字节跟着板一起进 IndexedDB
  （新增 assets 表，DB v1→v2 只加表不动画板数据），卡面照常显示图、刷新还在、再导出去照样带着；
  真缺件时导入结果里 `assets.missing` 与 `notes` 会明说，不假报完整成功。
  顺带修掉一个更隐蔽的：`importBundle` / `createBoard` / `deleteBoard` / `putBoard` 这些
  「整份板列表的读-改-写」以前不拿锁，而 `writeWorkspace` 会删掉不在列表里的板——
  并发时后写的那个等于把前一个的成果整块抹掉。
- **恢复备份的「取消」现在真的取消**：老实现是一句 `window.confirm`，点「取消」并不取消，
  它只是退回「合并恢复」，照样按 id 覆盖已有的板。改成选好文件先算一份**影响摘要**
  （备份里几块板 / 几块会覆盖同 id / 几块是新增 / 本机有几块不在备份里），再三选一：
  **取消**（零写入）/ **合并恢复**（同 id 覆盖）/ **整库恢复**（只有浏览器库有，备份里没有的板一并删掉）。
- **导入参数拼错当场 400 且零写入**：`?mode=restroe` 以前静默退回默认的 `copy`，
  于是「恢复备份」变成「又复制了一份」，而调用方从 201 里看不出意图被改过。
  现在显式给了就必须认识（query 与 body 同一套校验），不传仍是默认值。
- **两边认同一种文件**：`GET /api/boards/export?format=json` 返回的是 `{ok,bundle}` 包装，
  而「把响应存成文件」是最顺手的备份方式。以前只有服务端的导入路由剥了这层壳，浏览器库不认，
  同一份文件一边收一边拒。现在剥在**共享解析入口**里；存下来的若是一次**失败**的导出
  （`{ok:false,error}`），报的也是「这份文件存的是一次失败的导出」而不是「找不到画板」。
- 回归：smoke 补 199/200/201 边界、分卷计划与逐卷取回的覆盖完整性、越界卷号、
  html/md 同口径、种子+子板递归+环状引用、非法 mode/onConflict 的零写入、`{ok,bundle}` 包装；
  新增 `e2e/backup-restore.spec.ts` 三条（浏览器库附件往返与刷新、恢复三选一含取消零写入、
  服务端库的恢复只给两档）。
### 显示与界面审查修复（顶栏窄屏 · 分组框归属 · 阅读顺序 · 子板元数据 · 连线标签）

- **顶栏窄屏不再把命令挤出屏幕**。以前顶栏里的按钮全是 `flex:none`，放不下就直接站到可视区外面
  （实测 778px 宽的窗口上「整理 / 阅读 / 导出 / 评论 / 历史 / 设置 / Agent」全在屏幕外，整页还多一条横向滚动条；
  1600px 上因为站点导航展开了七个标签，同样溢出 108px）。现在改成**量出来的收纳档位**
  （`components/TopBar.tsx` 的 `useCompactLevel`）：装不下就升一档，按「先收字 → 模板/卡片 →
  历史/设置 → 整理/导出 → 评论/Agent → 品牌字与手势开关」的次序把命令整批挪进新的**「更多」**菜单，
  「阅读」留到最后一档。为什么不用断点：板名多长、有没有面包屑、界面是中英哪种，都会改变顶栏需要的宽度，
  断点只能猜一个视口宽度。站点导航同步支持收起态（`SiteNav` 的 `compact`），最窄时收成一颗，点开还是全部页面。
  320–1920px、中英两种语言、侧栏展开 / 收起都不再有横向溢出，e2e 按宽度扫一遍守着。
- **分组框：把「圈住了但没归属」摆到明面上，并给一颗显式收纳**。归属的真源仍然只有子卡的 `frameId`——
  系统**不会**因为两个矩形重叠就替你改归属。框上（以及右键菜单、阅读模式里）会报出差额并给一颗
  「收纳 N 张」，点了才收；只收还没归属任何框的自由卡，不从别的框里抢，判定是「卡片中心点落在框里」。
  它走批量口，因此服务端先打一份整板快照、往工作日志记一条，提示条上给一颗**撤销**。
- **阅读顺序有了三个把手，且不设时与从前逐张一致**：① **章节**——分组框读成一章，框里的卡跟着它的框走，
  目录里缩进挂在它下面；② **跳过**（`card.reading.skip`）——把侧边服务信息、意见入口这类附录移出阅读序列，
  阅读模式头上就地一按即可，只影响阅读顺序，导出 / 大纲照旧收全板；③ **显式序号**（`card.reading.order`）——
  几张卡钉到最前，其余仍按摆放位置跟在后面。不变量：一张卡都不会丢，归属指向一个已删除的框时按自由卡照常读到。
  顺带修正帮助文档里「阅读顺序按连线走」的错误说法（按连线走的是**大纲**）。
- **子画板卡的元数据实时了**：卡数与目标板名从 `getState()` 读一次改成**订阅** primitive，
  目标板增删卡 / 改名当场跟着变；卡片自定义标题（建卡时抄下的别名）保留不动，
  跟目标现名不一致时把现名并排摆出来，避免「点进去发现不是那块板」。
- **连线标签更读得出来**：标签沿法线推离线身、同一对卡片之间的多条线把标签沿曲线错开
  （**只挪标签，线的走向与语义一个字节没变**）、长标签截断 + 选中时展开完整文字、底色不透明加一圈细边。
  真正的避障（折线路由 + 占位图）代价太大且会改变现有每一条线的走向，后续方案写在
  `components/FloatingEdge.tsx` 尾部与帮助的「连线」一页。

### 开源可用性与可靠性审查

- 新增 `/agent`、部署专属轻量 Skill 下载、Codex/Claude 安装指令；画板列表和画布右键可复制带 ID、深链、动态指南的 Agent 提示词，复制失败如实反馈。
- 对齐与等距保留精确几何：统一 22px 格距，边/中心基准线先吸格再计算坐标，修复 tidy 换行后再量化导致的错位。邻居吸附覆盖缩放、多选、分组框内距，松手与刷新不再丢掉吸附结果。
- 大纲、聚焦、对齐、网格与快捷条新增自定义悬停 / 键盘聚焦说明卡，显示用途与当前开关状态；帮助新增“画布辅助开关”，讲清手动网格吸附、邻居对齐和整板“网格铺开”的区别。
- 生产进程与构建使用独立预留标记互斥，避免在线构建被覆盖后静态资源失效；doctor 同时检查首页 JS/CSS 与 MIME。
- 修复失败写入污染画板/Issue/Runner 内存状态、损坏配置被后续写入覆盖、ACP 畸形消息崩溃与无界帧、导出表格属性注入、私网嵌入域名误判；错误诊断不再回显秘密片段。
- 带 token 的 Runner/MCP/诊断请求拒绝重定向；Runner 设置读取复用鉴权通道。新安装默认仅监听 loopback，局域网共享需显式配置。
- 源码发布校验实际固定提交，禁止私人运行数据和未经审核的二进制资产，输出不带 Git 历史的源码包与 SHA-256；占位 npm 包不再被文档当成可用安装入口。
- 新增安全、几何、Agent 安装/复制、构建并发与发布边界回归。可选知识库/图书来源继续由通用协议接入，默认关闭，不附带私人服务实现或内容。

### 新增

- **界面中英双语 + 顶栏「设置」**：语言是**每个浏览器各选各的偏好**，所以真源是一枚 cookie
  （`blotboard.locale`），根布局在服务端读它，一次写进 `<html lang>`、`<body data-locale>` 与
  LocaleProvider 三处——服务端渲染那一遍就已经是对的语言，英文用户不会先闪一下中文。
  没选过的按浏览器 `Accept-Language` 猜一次，选过之后一律听用户的。
  字典按界面区域拆成多份（`lib/i18n/dicts/*.zh.ts` + 同名 `.en.ts`），中文那份是键的唯一来源，
  英文那份用 `Record<keyof typeof xxxZh, string>` 钉死——**漏译一条 typecheck 当场红**，
  不用等用户看见空白；几个人同时补翻译也不会撞在同一个文件上。
  卡片包目录里不塞第二种语言：中文名仍是各包自己的 `meta.label`（服务端导出与信封都吃它），
  英文走字典的 `card.<type>`，加一种卡片时不必先学 i18n。
  顶栏右侧新增**设置**入口：语言、画布三个辅助开关（与工具箱同一份 store，改哪边另一边当场变）、
  数据存在哪里、以及这台部署的自我介绍（版本 / 任务后端 / 可选集成 / 三个自描述接口）。
- **帮助文档中英双语**：译文放 `docs/guide/en/`，文件名即 slug 与中文一一对应；
  `/docs` 与 `GET /api/docs` 跟着 locale cookie 走，agent 那边没有 cookie 就用 `?lang=en`。
  页的**清单**永远以中文目录为准：某一页还没译就**按页**退回中文原稿，不是整站退回也不是 404——
  译到哪儿用到哪儿。
- **英文 README**（`README.en.md`）：与中文版同结构、同表格、同事实，两份互相链接。
- **MCP 工具带上 annotations**：18 个工具各有 `title` 与 `readOnlyHint` /
  `destructiveHint` / `idempotentHint` / `openWorldHint`，客户端能更准确地展示
  「这个工具大概会干什么」。**它是展示提示不是权限校验**——真正的闸门仍在画板那边
  （`x-auth-key`、停用的卡片包、并发 409、自动快照）。
- **动态指南里多了「三条最短路」**（`focus=api` 与 `/agent` 页链的快速指南都带它）：
  批量往板上加内容（信封 `/ingest`）、导入导出一份文件（`&download=1` 给的就是标准画板包）、
  只改布局（`/tidy` 与纯几何的 `/state`），各一段照抄就能跑的 curl 与返回字段说明。
- **导入导出画板（画板包 · `lib/board-bundle.ts` + `lib/board-transfer.ts`）**：一块板、一个分组、
  整个库都能打成一份 `.blotboard.json` —— 板名、分组、卡片、连线、评论、视口与**附件字节**
  （base64，总量 24 MB 封顶，超限的逐条说明而不是静默丢件）一起带走，
  在另一台画板上 `POST /api/boards/import` 就立起来。
  · 出口：`GET /api/boards/export?ids=|group=|all=1&format=json|html|md` 与
  `…/{id}/export?format=bundle`；**默认把子画板与被 board 卡指到的板一起带上**——
  不带走的话，对端点开那张卡就是死链。
  · 入口宽进：画板包、单块板 JSON（`format=json` 的产物）、旧的浏览器库备份，
  以及**排版导出的那份 HTML**——产物末尾带着同一份载荷，于是「发给别人看的文件」
  与「别人能导进自己画板的文件」是**同一个文件**。图片不存第二份字节：
  正文里那张 data URI 的 `<img>` 标了 `data-asset=<上传件 id>`，导入侧照它还原。
  载荷严格等于**这份文件里看得见的东西**（开着筛选就只带命中的那批，没勾「带评论」
  就一条批注不带）——「顺手能导回来」不该变成「顺手多发了没打算给的内容」。
  · 两种语义：`mode=copy`（默认）**一律新建**，卡片 / 连线换新 id、引用整体搬家
  （连线两端、评论目标、分组框归属、子画板卡指向），任务卡**不继承**来源机器的 Issue 与任务 id；
  `mode=restore` 保住原 id 恢复自己的备份，撞上同 id 默认跳过，`onConflict=replace` 才覆盖，
  且覆盖前自动打一份快照（顶栏「历史」里能退回去）。
  · 界面：顶栏「导出 → 导出画板包」、左栏右键一块板 / 一个分组「导出…」、
  「新建」旁的「导入画板…」，存储面板里多了「备份整库 / 导入画板 / 从备份恢复」一组
  （**服务端库与浏览器库同一种格式，可以互相导**；浏览器库的旧备份文件照样能恢复）。
  · 也给 agent：`/api/capabilities` 多一节 `transfer`，MCP 多一个 `board_import`、
  `board_export` 多一种 `format=bundle`。

- **一键 PDF 导出（`导出` → `导出 PDF` / `PDF 排版设置…`）**：**分页与版面在浏览器这一侧算**，
  产物是一份真正分好页的文档，再交给系统打印（对话框里选「另存为 PDF」）。
  纸张 / 方向 / 页边距 / 字号 / 分栏 / 封面 / 目录 / 页脚页码 / 每张卡另起一页 / 范围
  （整块板 · 筛选命中 · 画布上选中的那几张）/ 带评论 / 印不印图 / 链接后附网址——
  设置存在浏览器本地（`lib/pdf-settings.ts`）。设置面板右边是**真实分页预览**：
  与打印产物同一份文档、同一次分页，改一项页数当场变。
  保住的东西：**链接是真链接注解、文字能选能搜**（矢量，不是把每页截成图）、图片内联进产物、
  卡片不跨页（超过一整页的长卡让它自己往下流）、目录里是**真实页码**。
  实现分三层：服务端只切「不该被拆开的最小块」（`lib/export-print.ts` + `?format=print`），
  浏览器量高度装页（`lib/export-pdf.ts`，往页里塞、塞不下就退回来另起一页）——
  **目录页也走同一条量着装的路**：一开始它按「一页 30 条」硬切，而 A4 标准边距只装得下 20 多条，
  多出来的几条被 `overflow:hidden` 悄悄裁掉（目录里凭空少几行，还留下一行被切掉半截的字）；
  现在目录与正文同一套量法，e2e 里加了「没有任何一页装超」的不变式守着，
  内容口径与单文件 HTML 导出共用 `prepareExport` 与卡片包的 `export.ts`——**两种产物永不漂移**。
  顺带给 html / print 两种导出加了 `&ids=` 口径（只导指名的那几张）。
- **帮助文档（`/docs`）**：随服务走的一整套用户文档——介绍 / 五分钟上手 / 界面导览 / 快捷键 / 画布辅助开关 /
  21 种卡片 / 连线语义 / 分组框与子画板 / 模板 / 十一种整理 / 三种看法 / agent 接入 /
  评论交活 / 任务与 Issue / 快照回滚 / 导出 / 规格与信封 / 资源库 / 常见问题，共 20 页。
  正文真源是 `docs/guide/*.md`（标题 / 摘要 / 分组 / 序号写在 frontmatter 里，运行时读目录），
  **加一页 = 往目录里放一个 .md，代码一行不用改**；页面有分组目录、连正文一起匹配的搜索、
  站内跳转与上一页 / 下一页；agent 走 `GET /api/docs`（`?slug=` 取单页、`&format=md` 拿裸 Markdown）
  拿同一份。与 `cards/<type>/skill.md` 分工明确：那份讲「怎么调接口」，这份讲「人怎么用」。
- **引用资源库**：一类新内置规格 `agent-skill`（引用资源）——登记能被 agent 直接装上用的
  skill / MCP / CLI / 库，字段带安装方式、适配的 agent、触发词与验证状态。真源就是画板上的
  规格卡，不另设存储；新增跨板聚合视图 `/resources` 页（类型 / 状态筛选、安装方式一键复制、
  深链回卡）与 `GET /api/resources`（`?q=&kind=&status=` 过滤），两者同一份数据。
  `/api/skill` 指南新增 `resources` 一节（`?focus=resources` 单取），`/api/capabilities`
  新增 `resources` 能力块，画板顶栏与导航同款入口。登记口径：agent 发现好东西就建一张
  `agent-skill` 卡（信封带 `source.externalId` 判重），状态没实测过就写「待装」。
- **画板内核**：一板一文件的 JSON 存储（原子写 + mtime 感知外部改动）、公共 schema
  与按类型分派的归一化、REST API 全套（板 / 卡 / 连线 / 评论 / 状态 / 导出 / 导入）。
- **21 种卡片包**：每种卡片一个 `cards/<type>/` 目录（meta / schema / export / ui / skill.md
  五件套），注册表驱动；卡片中心可逐个开关，停用只挡新建、已有卡片一字不丢。
- **音视频卡（`media`）**：本地音频 / 视频拖进画布就地播，卡面是带进度条的播放器，
  阅读模式摊成大舞台。收 `mp3 / m4a / aac / wav / ogg / oga / opus / flac` 与
  `mp4 / m4v / webm / mov / ogv`（图片同时加收 `avif` / `bmp`），单文件上限
  `BLOTBOARD_MEDIA_MAX_MB`（默认 200 MB）。上传**流式落盘**（内存占用与文件大小无关）、
  下载口**支持 Range 分段**（拖进度条才有效；没有 206 Safari 干脆不播），
  校验仍是「扩展名 + MIME + 字节签名」三重，`kind` / `mediaType` 由服务端按后缀推导，
  调用方写错也纠得回来。嵌别人网站上的播放页仍走网页嵌入卡（`html`）。
  允许格式表收到唯一真源 `lib/upload-accept.ts`（服务端校验与 `<input accept>` 从同一张表派生，
  不会再出现「选得中、传上去 415」）。
- **规格卡与信封**：一份 JSON 规格 = 一类结构化卡片（16 份内置，可自建），
  批量收发走统一信封格式，带干跑校验与 externalId 判重。
- **改板安全网**：每个批量写入口动手之前自动整板快照（每块板留最近 10 份），
  历史抽屉里一键回滚（回滚前再存一份）；同处记 agent 的工作日志。
- **任务体系**：任务卡转 Issue、发起执行；内置 local 后端开箱即用，也可接任意
  `docs/RUNNER.md` 协议的 http 后端；`/tasks` 任务台管镜头 / 权限请求 / 孤儿 Issue。
- **ACP 派单**：local 后端能直接拉起本机 coding agent（Agent Client Protocol），
  流式 transcript、权限确认、中止都回到任务台。
- **MCP server**：`npx blotboard mcp`，17 个 `board_*` 工具覆盖日常改板动作。
- **三种看法与十一种整理**：画布 / 大纲 / 对比，加上从「整齐化」到「四象限 / 泳道 / 子图分簇」
  的服务端整理算法（`POST /tidy` 与顶栏菜单同一套，都能撤销）。
- **阅读模式全屏**（`F` / 弹窗头上的「全屏」）：版面铺满整块屏——图 / PPT / 表格的舞台按
  可视高度重算（`--reader-view-h` / `--reader-stage-h` 两个变量），正文栏放宽一档、字号提一档
  （不铺到最宽：一行超过九十来个汉字，眼睛回到下一行行首就要找）。原生全屏进不去的浏览器
  （iOS Safari 没有元素全屏）自动退回「铺满窗口」，功能不缺一块。全屏只在**原地**加个类 +
  请求全屏、不搬 DOM——搬一下里面的 iframe 就会重载，翻到第 7 页的 PPT 会跳回第 1 页。
- **阅读模式的键盘归属**（正文里嵌了别人的页面时才出现）：焦点一旦掉进 iframe，
  ←/→ 与 Esc 就静默失效且界面上毫无迹象。现在默认把焦点抢回来保证翻卡，
  要用 ←/→ 翻那张 PPT 就按头上的「键盘」开关明着交出去，换一张卡自动收回。
  「有没有嵌入页」是**按 DOM 探测**的（不认识具体卡片类型），以后哪个卡片包嵌了页面自动跟上。
- **导出**：单文件自包含 HTML（离线可开、打印即 A4 PDF）/ Markdown / JSON / 卡片信封。
- **`/api/skill` 按部署现场拼装**的 agent 指南 + `/api/capabilities` 机器可读能力清单。
- `BLOTBOARD_CHECKPOINT_ORPHAN_TTL_DAYS`（默认 30，`0` = 永不清）：画板删掉之后，
  它的快照目录再留多久才清。留是为了「删错了还能整块捞回来」，清是因为没人会再动它。
- `BLOTBOARD_TRUST_PROXY`：显式开启后才信 `x-forwarded-host` / `x-forwarded-proto`。
- `CONTRIBUTING.md` / `SECURITY.md` / 本文件；README 增「放到公网前想清楚」一节。
- **`npm run lint:repo` 仓库静态体检**（`scripts/lint-repo.mjs`，纯 Node 零依赖、秒级）：
  七项检查一一对应这个项目真栽过的跟头——源码里的裸 NUL（会让 `grep -r` 悄悄跳过整个文件）、
  私有痕迹（本机路径 / 私有端口 / 工单号 / 身份串，身份模式运行时从本机推导，不写进源码）、
  文档与注释里的死链、卡片包完整性（**声明了 `meta.fieldKey` 就必须有 `ui.FullView`**，
  否则阅读模式一片空白）、env 与 README 对账、CSS 变量、
  「读运行时 env 的页面没写 `force-dynamic`」。每条红都给「文件:行号 + 为什么 + 怎么改」。
- **`npm run check` 一条命令跑全套**：`lint:repo → typecheck → build → smoke → e2e`，
  便宜的先跑，任一步失败立刻停并说明这一步是干什么的、红了通常意味着什么。
  提交前的要求从「四条命令」收敛成这一条（四条并列时漏跑是常态）；CI 也改成直接调它。
- **`npm run doctor` 一键诊断**（`scripts/doctor.mjs`，只读）：打活服务出一份可直接贴进 issue 的
  Markdown 报告——版本 / 端口实际落位 / 数据目录 / token 生效**来源类型**（绝不打印值）/
  三个可选集成的可达性 / 启用的卡片包与规格 / 板数与坏板文件 / 快照占用与孤儿目录 /
  磁盘余量 / 最近一次写入。地址发现与 MCP 同一套三级逻辑；连不上时给排查步骤而不是堆栈。
- **透传铁律的属性式冒烟用例**：按固定种子生成一批刁钻卡片（未知类型带多层嵌套自定义字段、
  六万字长文、含 NUL/emoji/RTL/引号/反斜杠的字符串、极值坐标、空数组与 null），
  走 `PUT /whole` 落盘再读回逐字节比对，并加一轮「读→写→读」幂等断言。
  失败时打印可复现的种子（`SMOKE_SEED=… npm run smoke`）。
- **缩放卡片时也认邻居对齐**（工具条「对齐」开关同一个）：以前只有拖动会贴齐，
  拉边只能靠肉眼凑——一排卡片位置对得整整齐齐，宽度总差那么几像素。
  现在**手上拉的那条边**（以及跟着走的中线）会贴上邻居的边线与中线并画出参考线，
  对面那条边纹丝不动，斜角手柄两条轴各贴各的。容差量在**把手**上而不是线上：
  中线走得比手慢一半，所以它要差到 3px 才吸得动，不会出现「看着只差一点点、卡片猛地缩掉十几像素」。
  等距吸附不参与缩放——它解的是「整块摆在哪」，对「这条边拉到哪」没有意义。
  松手不再弹回：React Flow 收尾时会补一帧它自己算的原始尺寸，现在换成最后一帧吸附后的值；
  只开网格时，没吸上的那条轴按**被拉的那条边**的绝对坐标落到 22px 刻度上（跟拖动量左上角是同一把尺），
  并且吸附与量化都认卡片尺寸上下限（与缩放手柄共用 `CARD_SIZE_LIMITS`，不会被参考线推出手柄的范围）。

### 变更

- **`/llms.txt` 改为现场拼装，不再落静态文件**：原先根目录有一份手写的 `llms.txt`，路由启动时读它。
  那份文件写的是某一台机器的地址、端口与开关——属于部署状态而不是源码（`scripts/source-policy.mjs`
  的发布边界据此拒收它，`lint:repo` 一直红着），而且改了端口或换了任务后端就当场过期，读它的 agent 无从察觉。
  现在与 `/api/skill` 同一套路（`lib/llms-txt.ts`）：接口表、坑、文件入口这些仓库知识写在代码里随仓库走，
  地址、端口、启用了几类卡片包、任务后端是哪种、token 在哪，每次请求按当前配置现算，链接跟着调用方
  访问的 host 走。这个口免鉴权，所以与 `/api/health` 同一条红线：**绝对路径不进正文**。

- **左栏画板菜单的「移动到」收成二级**：右键一块板、或点它的 `⋯`，分组不再一个一个平铺在一级菜单里
  （分组攒到二三十个时，那份菜单比窗口还高，复制 ID、删除这些常用项被顶出屏幕）。现在只留「移动到」一行，
  右边跟着当前所在分组；鼠标停上去（触屏点一下）才在旁边摊开具体分组，列表长了自己滚，一级菜单长度不再随分组数变。
  「新分组…」「移出分组」「提为顶层画板」也一并收进这一层——它们本来就是同一类动作。
  二级菜单是 `ContextMenu` 的通用能力（`MenuItem.submenu`）：右边放不下会翻到左边，下边放不下会往上顶。

- **顶栏按心智重排成三块**：左上角固定是「去哪儿」（站点导航：画板 / 导航 / 资源 / 任务台 / 帮助，
  当前页高亮，从画板出发新标签打开），正中是「怎么看这块画布」（选择 / 抓手 + 缩放 / 适应 / 原点 / 刷新），
  右侧是「对这块板做什么」的三个分组胶囊（内容：模板 · 卡片｜整理与导出：整理 · 阅读 · 导出｜
  协作与 Agent：评论 · 历史 · Agent）。此前「导航 / 资源 / 任务台」三颗页面入口散在右半边，
  与「整理 / 导出 / Agent」这类板级动作混排——两类心智混在一排，是顶栏又长又难认的根因。
  窄屏按三档收字（先收站点导航的非当前页、再收右侧分组、最后收当前页），**分组的分界一直在**。
  顺带去掉品牌位上的「独立服务」角标与子页面上的「子页面」角标——当前页由站点导航自己交代。

### 修复 / 加固

- **整板改写不再顺手剪掉连线上的批注**：`PUT /whole` 以前给每条边无条件重发 id，
  于是最保守的写法——GET 整块板、一个字不改、原样 PUT 回去——也会把挂在连线上的评论
  连根剪掉（评论按存活 edge id 过滤，id 一换就全成了悬空的），而指南写的是
  「`comments` 不传 = 一个字不动」。口径改成 **同一对 from/to 就是同一条线**：
  端点没变的边保住原 id 与建立时间，真被删掉的边照旧连它的批注一起清。
  搬家那条路本来就是保 id 的，这次两边一致了。
- **MCP 导出不再把机器要吃回去的产物截断成「成功」**：`board_export` 的 bundle /
  整板 JSON / 卡片信封超过单次输出上限时**报错**，并给出能拿到完整产物的确切 HTTP 路径
  （`GET /api/boards/export?…&download=1`，免鉴权，落地即可 `board_import`）与收窄办法；
  以前这里返回 `isError=false` + 一句「已截断」，那份 JSON 根本解析不了，
  agent 会把坏文件当备份存起来。给人读的 markdown 摘要照旧可以截断。
- **MCP `board_export` 的导出范围必须明确选一个**：没给 `board_id` / `group` 时以前默认
  `all=1`，于是显式写了 `all: false` 的调用照样把整个库导出来。现在三选一缺一个就报可操作的参数错误，
  只有 `all === true` 才导全库，给多个范围也会被挡下。
- **`?focus=` 写错时会指路**：`SKILL_FOCUS` 是按活分的，「只改布局」这类活没有同名 focus
  （整理归在 `api` 那节），光回一句「不是合法取值 + 十个候选」等于让 agent 自己猜。
  400 照旧（不做静默别名），但把正确的去处一并说清；`/api/capabilities` 的 `skillFocus`
  多了 `notSections` 自描述这几类活归在哪节；`lint:repo` 新增第 10 项，
  仓库里任何字面量 `?focus=` 的值都要是 `SKILL_FOCUS` 里真有的。
- **「新建」菜单不再长过屏幕**：分组攒到二三十个时，平铺的「在「X」里新建」把菜单撑得
  比窗口还高，排在它后面的「导入画板…」与「新建分组…」直接掉到视口外面点不着——
  真实的库（25 个分组）上那个刚做好的导入入口等于不存在。分组收进二级菜单
  （与「移动到」同一套做法，二级菜单自己会滚），一级永远只有那几行；
  e2e 里加了「一级菜单不许长过窗口」的用例守着。
- **原子写收口成一个 helper**：写或 rename 失败时兜底删掉临时文件（原先失败会在数据目录里
  留下永远没人认领的 `.xxx.tmp`）；六处各抄一份的 tmp+rename 全部改为调用它。
- **被删画板的快照目录不再是纯泄漏**：板不在了且目录闲置超过 TTL 才整个清掉，
  进程内首次列板时懒触发。
- **关掉 IP 闸门时启动会大声警告**（`BLOTBOARD_ALLOW_PUBLIC_DIRECT=1` + 默认监听 `0.0.0.0`
  等于任何人都能读写你的画板）。
- **`x-forwarded-*` 默认不再被信任**：导出与 `/api/skill` 里回指画板的 base
  只在 `BLOTBOARD_TRUST_PROXY=1` 时才认这两个头。
- **ACP transcript 有了硬上限**（8 MB）：写满停笔并留一条「已截断」记录，
  跑飞的 agent 不会把磁盘写满。
- **local 后端生成的 prompt 不再带本机绝对路径**（那段会随免鉴权的 `GET /api/issues/:id` 吐出去）。
- 源码里两处用作分隔符的裸 NUL 字节改成 `\u0000` 转义——埋着的时候 `grep -r`
  会把整个文件当二进制**悄悄跳过**，排查时「明明有这个字串却搜不到」。
- CI 的 Node 版本锁到一条 LTS 线（原先写 `20`，runner 可能给到装不动 Next 16 的 20.0–20.8）。
- e2e 不再依赖开发机上跑着的外部服务，干净机器上也能全绿。

### 已知技术债（记在这里免得忘）

- 几个大文件该拆了（`lib/board-service.ts` 一千五百行、`scripts/smoke-api.mjs` 更长）。
- 归一化层里还有一批 `any`（历史上是「强制 clamp + 兜底」风格的直译，收紧要连着测试一起改）。
- 上传件没有签名 URL / HMAC，只靠不可枚举 id + 网络边界（见 SECURITY.md）。
- `lib/layout.ts` / `outline.ts` / `board-service.ts` 里按 `card.type` 分支的领域知识
  （timeline / kanban / matrix / 任务索引）还没抽成注册表钩子，见 AGENTS.md 里那条「已知例外」。
- `package.json` 仍是 `private: true`，npm 发布相关字段等真要发的时候再补。

### 整理实验室与持续撤销

- 全部11种整理统一检查输出完整性、坐标范围和固定区避让；孤儿frameId不再让卡片永久退出整理。超界布局原子拒绝，显式未知mode返回400，计算期间有并行修改返回409；新增changed字段保留moved旧口径。
- 整齐化修复连锁吞列、交叉震荡与重复整理漂移；流程修复分支/汇合倒流；网格统一单元步长，类型分区保住最小留白；dagre重复边采用稳定的最强权重。
- 时间线统一UTC自然日；四象限修正缺失数值、溢出中位数、enum值/标签歧义及未知状态分类；看板顺序不再依赖输入数组排列。
- 新增有界持久化差量编辑历史、连续撤销/重做与历史面板；每次整理独立记录，纯视口不记录，连续同字段文字编辑合并。保留整板checkpoint作为灾备。
- 新增Ctrl/Cmd+Z、Shift+Z与Ctrl+Y和可发现的快捷键帮助；输入、IME、弹窗与嵌入编辑器保留自己的键盘与粘贴操作。
- 新增实际API布局实验脚本，仅显式允许后创建专用测试板；覆盖固定分组框、异形卡、分支汇合、孤卡及重复稳定性。
- 网格按卡数与尺寸选择约16:10的均衡行列；单/多选浮动工具条和适应视野按实际屏幕障碍避让，顶部卡片不再被工具箱挡住。
